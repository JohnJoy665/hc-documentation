# Открытые вопросы

## PDF и OCR

- TODO: нужно проверить PDF `raw/2026-06-17/pdf/node-card.pdf` вручную или через OCR. Автоматическое извлечение текста дало неинформативный результат.
- TODO: нужно проверить PDF `raw/2026-06-17/pdf/node-authentication-settings.pdf` вручную или через OCR. Автоматическое извлечение текста дало неинформативный результат.
- TODO: нужно проверить PDF `raw/2026-06-17/agents/agent-admin-settings.pdf` вручную или через OCR. Автоматическое извлечение текста дало неинформативный результат.

## Платформа SP-XML

- TODO: нужно проверить соответствие старой объектной модели WebTutor конкретным каталогам WebSoft HCM.
- TODO: нужно проверить актуальные имена исполняемых файлов в новых версиях WebSoft HCM, где встречается `xhttp_64.exe`.
- TODO: нужно проверить, какие параметры `SpXml2.ini` и `xHttp1.ini` реально используются на локальном стенде.
- TODO: нужно собрать отдельные RAG-заметки по строковым функциям `StrLen`, `StrCharCount`, `StrLeftCharRange`.
- TODO: нужно проверить полный список типов SP-XML Script.
- TODO: нужно проверить рекомендации по объявлению функций и библиотек в актуальной документации WebSoft HCM.
- TODO: нужно проверить точное имя секции `shared_doc` или `shared_docs` в актуальных файлах WebTutor.
- TODO: нужно проверить примеры библиотек V1/V2 именно в WebSoft HCM.
- TODO: нужно проверить актуальность `x-app://` для локальной установки.

## Встроенные функции SP-XML

- TODO: нужно сверить спорные места в строковых функциях: `StrCharRangePos()`, `StrSplit2()`, `StrReplaceOne()`, описание `StrUpperCase()`.
- TODO: нужно проверить безопасные практики использования `Eval*`, `HttpRequest()`, `ProcessExecute()`, `ShellExecute()`.
- TODO: нужно собрать отдельные рецепты по `OpenDoc()` + `Save()` для типовых объектов WebTutor.
- TODO: нужно проверить, какие функции доступны в конкретной версии WebSoft HCM.
- TODO: нужно проверить поддерживаемые схемы URL для `LoadUrlText()`, `PutUrlText()`, `UrlExists()` на стенде WebTutor.
- TODO: нужно проверить правила сохранения и инвалидации кэша XML-документов.
- TODO: нужно извлечь и проверить подробное описание `ParseJson()`.

## Базовые объекты SP-XML

- TODO: нужно проверить полный список базовых объектов `PlainSeq`, `SafeArray`, `Stream`, потому что в новых материалах они только перечислены.
- TODO: нужно проверить `WriteBuffStream` как актуальную замену `Binary`.
- TODO: нужно проверить сигнатуру `ClassObjectsArray.Add()`.
- TODO: нужно проверить возвращаемое значение `MethodReference.Call()`.
- TODO: нужно проверить `String.ToWinLineBreaks()` по первичному источнику.

## XQuery

- TODO: нужно проверить полный список каталогов и полей для локального WebSoft HCM.
- TODO: нужно проверить производительность `contains()` на локальной XML-базе и PostgreSQL.
- TODO: нужно проверить поведение `doc-contains()` по кастомным полям на текущем учебном PostgreSQL-стенде.
- TODO: нужно проверить, поддерживает ли локальная версия все примеры `some ... satisfies`.
- TODO: нужно проверить поддержку `Fields(...)` для нужных каталогов и версии WebSoft HCM.
- TODO: нужно добавить карточку каталога `collaborators` с точными полями.

## Агенты сервера

- TODO: нужно проверить полный формат карточки агента: поля расписания, режим запуска, права, логирование.
- TODO: нужно проверить, куда именно выводится `alert(...)` при запуске агента в конкретной версии WebSoft HCM.
- TODO: нужно проверить точные поля каталога `requests` и структуру `workflow_log_entrys`.

## Динамические методы и свойства объектов

- TODO: нужно собрать примеры безопасного применения `CallObjectMethod`, `GetObjectProperty` и связанных функций в серверном коде WebTutor.
- TODO: нужно проверить функции `GetObjectPropertyWithLock`, `SetObjectPropertyWithLock` и объект `Lock`.
- TODO: нужно проверить, какие типы объектов в WebTutor могут возвращать исключение при отсутствующем свойстве.

## Авторизация и узлы

- TODO: нужно проверить, какие дополнительные переменные карточки узла обязательны для `cookie / Form-Based` в разных версиях WebSoft HCM.
- TODO: нужно проверить точное поведение `loggedoff.html` для `portal_auth_type = ntlm`.
- TODO: нужно проверить, какие поля карточки узла соответствуют `portal_auth_type`.

## Серверные HTML-хендлеры и API

- TODO: нужно проверить полный список свойств `Request` и методов `Response`.
- TODO: нужно проверить, есть ли методы `Response.SetHeader`, `Response.ContentType` или аналоги.
- TODO: нужно проверить production-safe способ установки `Content-Type: application/json`.
- TODO: нужно проверить безопасный синтаксис белых списков для `class` и `action` во внутреннем серверном языке WebTutor.
- TODO: нужно проверить актуальность `DropFormsCache` и замену для новых версий.

## x-local и маршрутизация

- TODO: нужно проверить точное разрешение `x-local://` для `WebTutorAdmin` и `WebSoftServer`.
- TODO: нужно проверить влияние `SHARED-DATABASE` в `SpXml.ini` на разрешение `x-local://`.
- TODO: нужно собрать отдельные примеры коротких URL `/_wt/...` и их связи с объектами портала.

## xhttp_config.json

- TODO: нужно проверить актуальность параметров `xhttp_config.json` для конкретной версии WebSoft HCM.
- TODO: нужно проверить валидность JSON-примеров из PDF после ручной сверки.
- TODO: нужно проверить, где именно размещается `XAuthIdOptions` относительно секции `Authentication`.

## Локальный PostgreSQL-контур

- TODO: нужно проверить влияние ошибок `ProcessDocInCatalog() failed` на поиск и каталоги.
- TODO: нужно проверить причину отсутствия `libAnalytics` в учебном контуре.
- TODO: нужно описать безопасный rollback учебного PostgreSQL-контура.

## Dev-pack

- TODO: нужно детально обработать функции `Tools`, `Tools_web`, `Tools_library`, `Ms_tools`.
- TODO: нужно собрать отдельные документы по `libMain`, `libAnalytics` и другим библиотекам программного кода.
- TODO: нужно расширить XAML-справочник.
- TODO: нужно проверить разделы API v1/v3 по полным первоисточникам.

## Создание сотрудников

- TODO: нужно проверить обязательные поля XML-документа `collaborator` при создании сотрудника в конкретной версии WebSoft HCM.
- TODO: нужно проверить, нужно ли вручную заполнять `last_data` при создании сотрудника или эти поля обновляет стандартная логика документа.
- TODO: нужно проверить правила хранения поля `password` в документе `collaborator`: допустима ли прямая запись пароля или требуется стандартная функция/хеширование.
