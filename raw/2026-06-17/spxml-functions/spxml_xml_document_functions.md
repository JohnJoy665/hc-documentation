# SP-XML: Работа с документами XML

## Опции открытия XML-документа

Опции открытия XML-документа используются в функциях `OpenDoc()`, `OpenDocFromStr()` и др.

Опции имеют вид:

```text
param1='value1';param2='value2';...
```

Список возможных параметров:

| Параметр | Описание |
|---|---|
| `form` | URL формы, используемой при открытии (`String`). Форма, указанная в самом документе, будет игнорироваться. |
| `ignore-top-elem-name` | Игнорировать имя корневого элемента (`Bool`). Позволяет использовать одну общую форму для документов с разными именами корневых элементов. |
| `drop-namespaces` | Удалять названия пространств имен в названиях элементов (`Bool`). |
| `ui-text` | Поддерживать загрузку документов с многоязычными данными, загружая данные с языком текущего пользовательского интерфейса. |
| `format` | Формат встроенного конвертора для открытия данных в других форматах в виде XML. Поддерживаются форматы: `sv` (separated values), `win.ini`, `excel`, `smml`. |
| `delim` | Разделитель, используемый для формата `csv`. Можно задать символ явно либо использовать значения `tab` или `sem`. |
| `header-line` | Считать, что первая строка для формата `csv` содержит названия полей (`Bool`). |
| `lower-case-names` | Приводить названия полей для формата `csv` к нижнему регистру (`Bool`). |

## Опции экспорта XML-документов

Опции экспорта XML-документа используются в функциях `SaveToUrl()`, `SaveToStream()` и др.

Опции имеют вид:

```text
param1='value1';param2='value2';...
```

Список возможных параметров:

| Параметр | Описание |
|---|---|
| `inline-ext-objects` | Включать объекты с внешним хранением в экспорт (`Bool`). Позволяет экспортировать в один файл полное содержимое документа, включая объекты внешнего хранения, например прикрепленные файлы. |
| `tabs` | Форматировать XML с использованием символов табуляции и перевода строк для удобства чтения человеком (`Bool`). Значение по умолчанию определяется глобальной настройкой в ini-файле приложения. |

## Список функций

- `CreateDocByForm()`
- `DeleteDoc()`
- `FetchDoc()`
- `GetAllCachedDocs()`
- `GetCachedDoc()`
- `GetOptCachedDoc()`
- `LdsDeleteDoc()`
- `MoveDoc()`
- `ObjectDocUrl()`
- `ObjectIDFromUrl()`
- `ObjectNameFromUrl()`
- `ObtainUiDoc()`
- `OpenDoc()`
- `OpenDocFromStr()`
- `OpenNewDoc()`
- `RegisterSharedDoc()`
- `ReplaceCachedDoc()`
- `UpdateUiDoc()`

---

## `CreateDocByForm()`

Создает новый XML-документ по заданной форме, переданной в виде объекта `XmlForm`.

### Синтаксис

```text
CreateDocByForm( form )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `form` | `object` | Объект формы. |

---

## `DeleteDoc()`

Удаляет документ с заданным URL.

### Синтаксис

```text
DeleteDoc( url )
DeleteDoc( url, strictDelete )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | URL документа. |
| `strictDelete` | `bool` | Необязательный аргумент. Удалять документ сразу, минуя помещение его в корзину. |

### Пример

```text
DeleteDoc( 'x-db-obj://data/candidate/0x4DF75B9F13FE5160.xml' )
```

---

## `FetchDoc()`

Открывает XML-документ и помещает его в кэш документов.

Если документ с заданным URL уже находится в кэше, возвращается уже загруженный в кэш документ.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `String` | URL документа. |
| `options` | `String` | Необязательный аргумент. Опции открытия документа. |

### Результат

`Объект XmlDoc`

---

## `GetAllCachedDocs()`

Возвращает список всех XML-документов из кэша.

---

## `GetCachedDoc()`

Возвращает документ с заданным URL из кэша. Если документ с заданным URL в кэше отсутствует, функция завершается с ошибкой.

### Синтаксис

```text
GetCachedDoc( url )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | URL документа. |

---

## `GetOptCachedDoc()`

Возвращает документ с заданным URL из кэша. Если документ с заданным URL в кэше отсутствует, возвращает `undefined`.

### Синтаксис

```text
GetOptCachedDoc( url )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | URL документа. |

---

## `LdsDeleteDoc()`

Удаляет на сервере приложения документ с заданным URL. Используется в специализированном коде, предназначенном для синхронизации баз данных или обмена данными между базами.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `docUrl` | `String` | URL документа. |
| `options` | `String` | Необязательный аргумент. Опции. |

### Результат

`undefined`

### Пример

```text
LdsDeleteDoc( 'x-db-obj://data/candidate/042D8A4596B679/E0.xml', 'lds-server=test2.datex.ru:9000' )
```

---

## `MoveDoc()`

Перемещает XML-документ из одного URL в другой URL. В отличие от обычного перемещения файла, действие этой функции сопровождается выполнением стандартных свойств `OnSave`, `OnBeforeSave` и т.д. Документ сначала пересохраняется по новому URL, затем удаляется из предыдущей. Редко используемая функция.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `String` | URL существующего документа. |
| `newUrl` | `String` | Новый URL документа. |

---

## `ObjectDocUrl()`

Возвращает URL объектного документа по имени базы, типа объекта и ID документа.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `dbName` | `String` | Наименование базы данных. |
| `objectType` | `String` | Наименование типа объекта. |
| `objectID` | `Integer` или `String` | ID документа. |

### Результат

`String`

### Пример

```text
ObjectDocUrl( 'data', 'person', 1238461 ) вернет 'x-db-obj://data/person/0x000000000012E5BD.xml'
ObjectDocUrl( 'data', 'event_type', 'interview' ) вернет 'x-db-obj://data/event_type/interview.xml'
```

---

## `ObjectIDFromUrl()`

Выдает ID объектного документа по его URL. Смотри также `ObjectDocUrl()`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | URL объекта. |

### Результат

`Integer` или `String`

### Пример

```text
ObjectIDFromUrl( 'x-db-obj://data/person/0x000000000012E5BD.xml' ) вернет 1238461
ObjectIDFromUrl( 'x-db-obj://data/event_type/interview.xml' ) вернет 'interview'
```

---

## `ObjectNameFromUrl()`

Выдает наименование типа объекта по его URL. Смотри также `ObjectDocUrl()` и `ObjectIDFromUrl()`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | URL объекта. |

### Результат

`String`

### Пример

```text
ObjectNameFromUrl( 'x-db-obj://data/person/0x000000000012E5BD.xml' ) вернет 'person'
ObjectNameFromUrl( 'x-db-obj://data/event_type/interview.xml' ) вернет 'event_type'
```

---

## `ObtainUiDoc()`

Пытается найти открытую в пользовательском интерфейсе карточку XML-документа. Если таковая карточка найдена, функция возвращает ссылку на документ из этой карточки. В противном случае действие функции аналогично `OpenDoc()`.

Чтобы сохранить измененный документ, необходимо использовать функцию `UpdateUiDoc()` (а не вызвать метод `Doc.Save`, как при открытии документа при помощи `OpenDoc`). Если документ был открыт пользователем на экране, то при выполнении функции `UpdateUiDoc()` документ будет изменен прямо на экране, если открытого документа не было — то документ будет просто сохранен.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `docUrl` | `String` | URL XML-документа. |

### Результат

`Объект XmlDoc`

---

## `OpenDoc()`

Открывает XML-документ. Возвращает объект типа `XmlDoc`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `String` | URL документа. |
| `options` | `String` | Необязательный аргумент. Опции открытия документа в виде `param1='value1';param2='value2';...`. См. опции открытия документа XML. |

### Результат

`Объект XmlDoc`

---

## `OpenDocFromStr()`

Открывает XML-документ, содержащийся в строке.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `dataStr` | `String` | Строка, содержащая данные открываемого документа. |
| `options` | `String` | Необязательный аргумент. Опции открытия документа в виде `param1='value1';param2='value2';...`. См. опции открытия документа XML. |

### Результат

`Объект XmlDoc`

---

## `OpenNewDoc()`

Создает новый XML-документ по заданной форме.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `formUrl` | `String` | URL формы. |

### Результат

`Объект XmlDoc`

---

## `RegisterSharedDoc()`

Загружает XML-документ в кэш документов и делает его корневой элемент видимым в списке глобальных имен.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `docUrl` | `String` | URL документа. |

### Результат

`Объект XmlDoc`

---

## `ReplaceCachedDoc()`

Заменяет документ с заданным URL в кэше документов на новый документ. Функция используется для серверных документов, к которым может идти обращение из нескольких потоков одновременно (например, глобальных настроек). Поскольку объект `XmlDoc` не является thread-safe, менять содержимое глобальных документов нельзя, однако можно заменить документ целиком, соблюдая thread-safety.

---

## `UpdateUiDoc()`

Сохраняет изменения в документе, открытом при помощи функции `ObtainUiDoc()`. Если это был документ, открытый пользователем на экране, то функция устанавливает аргумент метода `Doc.SetChanged(true)`, и больше ничего не делает. Установка этого аргумента необходима, чтобы при закрытии документа пользователю было предложено сохранить изменения. Если это был документ, открытый программой без участия пользователя, действие функции аналогично действию метода `Doc.Save()`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `doc` | `Объект XmlDoc` | Открытый документ. |

### Результат

`undefined`
