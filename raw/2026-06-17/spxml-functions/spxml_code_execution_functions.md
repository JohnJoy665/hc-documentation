---
title: SP-XML. Выполнение кода
source: pasted_text
status: draft
---

# SP-XML: Выполнение кода

## Объект

- `TaskProgress`

## Функции

- `CallCodeUrlFunction()`
- `CallServerMethod()`
- `CheckCodePageSyntax()`
- `CheckCodeSyntax()`
- `CodeLibraryMethodExists()`
- `CodeLiteral()`
- `eval()`
- `EvalAfterDelay()`
- `EvalCodePage()`
- `EvalCodePageUrl()`
- `EvalCodeUrl()`
- `EvalCs()`
- `ExtEval()`
- `ExtractUserError()`
- `GetOptCodeLibrary()`
- `InPlaceEval()`
- `IsCancelError()`
- `ModalTaskMsg()`
- `OpenCodeLib()`
- `OpenCodeLibrary()`
- `OptEval()`
- `RegisterCodeLibrary()`
- `SafeEval()`
- `StartModalTask()`

## TaskProgress

Объект, будучи созданным в произвольном месте выполняемого кода, позволяет задать новый уровень прогресс-индикатора. Прогресс-индикатор может быть отображен в UI, если код вызван с соответствующей опцией. В противном случае факт наличия прогресс-индикатора будет проигнорирован.

### Пример

```js
progress = new TaskProgress;
progress.TaskName = 'Loading ...';
progress.ActivityName = 'Preparing data';
progress.ItemCount = totalCount;
progress.CurrentItemIndex = 0;
progress.CurrentItemName = fileName;
```

### Поля

- `ActivityName`
- `CurrentItemIndex`
- `CurrentItemName`
- `ItemCount`

## CallServerMethod()

Вызывает метод (функцию) библиотеки на сервере приложения. У метода должна быть установлена мета-директива `"META:ALLOW-CALL-FROM-CLIENT:1"`.

Значения аргументов могут быть переданы либо через массив, либо через стандартный объект, содержащий пары `"имя аргумента"` - `"значения аргумента"`.

Не все типы значений могут быть переданы в серверный метод и возвращены обратно. Поддерживаются все скалярные типы, стандартные массивы, стандартные объекты, а также объекты `XmlElem` и `XmlDoc`.

### Синтаксис

```js
CallServerMethod( libName, methodName )
CallServerMethod( libName, methodName, argsArray )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `libName` | `string` | Имя библиотеки либо url библиотеки |
| `methodName` | `string` | Имя метода |
| `argsArray` | `object` | Необязательный. Стандартный массив значений аргументов, либо стандартный объект, содержащий значения аргументов |

## CheckCodePageSyntax()

Проверяет синтаксис страницы со вставками кода по правилам ASP. В случае неверного синтаксиса вызывает исключение с описанием ошибки.

### Синтаксис

```js
CheckCodePageSyntax( codeStr )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeStr` | `string` | Строка, содержащая вставки кода |

## CheckCodeSyntax()

Проверяет синтаксис кода на SP-XML Script. В случае неверного синтаксиса вызывает исключение с описанием ошибки.

### Синтаксис

```js
CheckCodeSyntax( codeStr )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeStr` | `string` | Строка, содержащая код |

## CodeLibraryMethodExists()

Проверяет наличие метода с определенным именем в v2-библиотеке. Возвращает `true` или `false`.

### Синтаксис

```js
CodeLibraryMethodExists( lib, methodName )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `lib` | `object` | Библиотека |
| `methodName` | `string` | Имя метода |

## EvalAfterDelay()

Выполняет код спустя заданный временной интервал. Доступно только на клиентской части приложения. Код выполняется всегда в главном потоке, независимо от того, в каком потоке была вызвана функция `EvalAfterDelay()`.

### Синтаксис

```js
EvalAfterDelay( codeStr, delay )
EvalAfterDelay( codeStr, delay, envObject )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeStr` | `string` | Строка, содержащая выполняемый код |
| `delay` | `integer` | Интервал в миллисекундах |
| `envObject` | `object` | Необязательный. Дополнительный объект, поля которого будут доступны в выполняемом окружении |

## ExtEval()

Выполняет код в заданном окружении.

### Синтаксис

```js
ExtEval( codeStr )
ExtEval( codeStr, envType )
ExtEval( codeStr, envType, baseObject )
ExtEval( codeStr, envType, baseObject, envObjects )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeStr` | `string` | Строка, содержащая выполняемый код |
| `envType` |  | Необязательный. Тип окружения: `"Global"` либо `undefined` по умолчанию, `"Safe"`, `"Doc"`, `"ScreenItem"` |
| `baseObject` |  | Необязательный. Базовый объект окружения. `XmlDoc` для окружения типа `"Doc"`, `ScreenItem` для окружения типа `"ScreenItem"`. Для остальных типов окружения необходимо передавать `undefined` |
| `envObjects` |  | Необязательный. Массив дополнительных объектов окружения, видимых по аналогии с конструкцией `with` |

## GetOptCodeLibrary()

Находит зарегистрированную v2-библиотеку по имени и возвращает ссылку на нее. Если библиотека с указанным именем не найдена, возвращает `undefined`.

### Синтаксис

```js
GetOptCodeLibrary( libName )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `libName` | `string` | Имя библиотеки |

## OpenCodeLibrary()

Функция `OpenCodeLibrary()` динамически загружает библиотеку v2. У динамически загруженной библиотеки v2, в отличие от статически описанной в модуле через `<code_source_files>`, есть два ограничения:

1. `Namespace`, указанный в файле, игнорируется.
2. Библиотека должна быть описана в одном файле.

### Синтаксис

```js
OpenCodeLibrary( url )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | Url файла `.bs` |

## RegisterCodeLibrary()

Динамически регистрирует файл v2-библиотеки, как если бы он был описан в модуле через `<code_source_file>`.

### Синтаксис

```js
RegisterCodeLibrary( url )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | url файла `.bs` или `.xmi` |

## SafeEval()

Выполняет код в безопасном окружении без доступа к каким-либо глобальным данными приложения.

### Синтаксис

```js
SafeEval( codeStr )
SafeEval( codeStr, envObjects )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeStr` | `string` | Строка, содержащая выполняемый код |
| `envObjects` |  | Необязательный. Стандартный массив, содержащий список дополнительных объектов, видимых по аналогии с `with {}` в окружении |

## CodeLiteral()

Кодирует аргумент как константу JScript. Используется для генерации выражений.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `value` | `Any` | Аргумент |
| `quoteChar` |  | Необязательный. Символ, используемый в качестве кавычки. Допустимые значения: одинарная или двойная кавычка. По умолчанию используется одинарная кавычка |

### Результат

`String`

### Описание

Значение типа `undefined` переводится в строку `'undefined'`.

Значение типа `null` переводится в строку `'null'`.

Значение типа `string` заключается в кавычки, при этом существующие кавычки внутри строки маскируются по правилам JScript.

Значение типа `integer` или `real` переводится в соответствующее строковое значение.

Значение типа `bool` переводится в строку `'true'` или `'false'`.

Значение типа `date` переводится в строку вида `'Date( '2011-01-30T10:30:00' )'`.

## eval()

Выполняет код JScript в текущем окружении. Аналогична стандартной функции JScript с таким же именем, но, в отличие от нее, выполняет код на отдельном уровне: переменные, объявленные внутри кода, не будут видны снаружи.

См. также `InPlaceEval`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `code` | `String` | Выполняемый код JScript |

### Результат

`Any`

## InPlaceEval()

Выполняет код JScript в текущем окружении. В отличие от функции `eval()` выполняет код на том же уровне: переменные, объявленные внутри кода, будут видны снаружи, и не позволяет возвращать значение.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `code` | `String` | Выполняемый код JScript |

### Результат

`undefined`

## EvalCodePage()

Интерпретирует содержимое страницы со вставками кода по правилам ASP.

### Синтаксис

```js
EvalCodePage( pageData )
EvalCodePage( pageData, options )
EvalCodePage( pageData, options, envType )
EvalCodePage( pageData, options, envType, baseObject )
EvalCodePage( pageData, options, envType, baseObject, extraEnv )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `pageData` | `string` | Строка, содержащая текст страницы |
| `options` | `string` | Необязательный. Строка, содержащая опции запуска в формате `"name1=value1;name2=value2"`, либо флаг `strictErrors` |
| `envType` | `string` | Необязательный. Тип окружения: `"Global"` либо `undefined` по умолчанию, `"Safe"`, `"Doc"`, `"ScreenItem"` |
| `baseObject` | `object` | Необязательный. Базовый объект окружения. `XmlDoc` для окружения типа `"Doc"`, `ScreenItem` для окружения типа `"ScreenItem"`. Для остальных типов окружения необходимо передавать `undefined` |
| `extraEnv` |  | Необязательный. Массив дополнительных объектов окружения, видимых по аналогии с конструкцией `with` |

### Описание

Поддерживаемые опции:

| Опция | Тип | Описание |
|---|---|---|
| `asp-style` | `bool` | Поддерживать стиль `<%=%>`. Включена по умолчанию |
| `bracket-style` | `bool` | Поддерживать стиль `[[]]` |
| `webbinder-style` | `bool` | Поддерживать стиль `::` |
| `strict-errors` | `bool` | При возникновении ошибки в какой-либо вставке, завершать выполнение всей страницы с этой же ошибкой. По умолчанию текст ошибки пишется в текст страницы, и выполнение продолжается |
| `caller-env` | `bool` | Выполнять в вызывающем окружении. Устаревшая опция, не рекомендуется использовать |
| `content-type` | `string` | Тип содержимого страницы. Поддерживается только `"text/xml"` |

Существует также старая нотация данной функции, когда вторым аргументом передается булево значение. В этом случае второй аргумент содержит не опции запуска, а единственную опцию `strictErrors`.

## EvalCodePageUrl()

Интерпретирует содержимое страницы по правилам ASP.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `pageUrl` | `String` | url, содержащий текст активной страницы |
| `options` | `string` | Необязательный. Опции |

### Результат

`String`

### Описание

Поддерживается единственная опция:

| Опция | Описание |
|---|---|
| `strict-errors` | Завершать функцию с ошибкой, если таковая возникнет при обработке страницы. По умолчанию, в случае возникновения ошибки ее текст добавляется к результату, а функция возвращается без ошибки |

## EvalCodeUrl()

Загружает код на JavaScript из заданного url и выполняет его.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeUrl` | `String` | url, содержащий код |
| `subCode` | `String` | Необязательный. Вспомогательный код, выполняемый после выполнения основного кода. Как правило, содержит вызов функции, описанный в основном коде |

### Результат

`Any`

### Пример

```js
EvalCodeUrl( 'rcr_lib_backup.js', 'RunBackup()' )
```

Использование данной функции для вызова функций, описанных в файле, не рекомендуется после появления функции `OpenCodeLib()`, предлагающей более понятные правила области видимости переменных:

```js
OpenCodeLib( 'rcr_lib_backup.js' ).RunBackup()
```

## EvalCs()

Выполняет код JScript аналогично функции `eval()`, но внутри т.н. критической секции, что исключает одновременное выполнение кода из разных потоков.

Функция как правило используется для доступа к данным, не являющимся thread-safe, например к глобальным XML-документам.

Данной функцией следует пользоваться с осторожностью, поскольку глобальная секция является общей для всего приложения. Не следует выполнять внутри критической секции код, который может занять продолжительное время: обращения к диску, сети и др.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `codeString` | `String` | Объект String, содержащий допустимый код JScript |

### Результат

`Any`

### Пример

```js
EvalCs( 'global_agents.agent_completed = true' )
```

## ExtractUserError()

Извлекает из объекта типа `Error` пользовательскую часть сообщения об ошибке. Если объект не содержит выделенной пользовательской части, возвращается полное описание ошибки.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `e` | `Error` | Ошибка |

### Результат

`String`

### Пример

```js
try
{
    HttpRequest( .... );
}
catch ( e )
{
    alert( 'Невозможно активировать программу: ' + ExtractUserError( e ) );
}
```

## ModalTaskMsg()

Обозначает текущий статус выполнения фрагмента кода с упрощенной индикацией пользователю, начатого путем вызова функции `StartModalTask()`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `msg` | `String` | Строка статуса |

### Результат

`undefined`

## OptEval()

Выполняет код JS, который, возможно, завершится с ошибкой, с возвратом заданного значения по умолчанию в случае ошибки.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `code` | `String` | Код JScript |
| `defaultVal` | `Any` | Необязательный. Значение, возвращаемое в случае ошибки. Если аргумент не указан, а код завершается с ошибкой, возвращается `undefined` |

### Результат

`Any`

### Пример

```js
OptEval( 'doc.TopElem.xxx', '' )
```

## StartModalTask()

Обозначает запуск длительного фрагмента кода с упрощенной индикацией пользователю.

Прогресс выполнения будет обозначаться путем периодического вызова функции `ModalTaskMsg()`.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `taskTitle` | `String` | Название задачи для пользователя |

### Результат

`undefined`

### Пример

```js
ModalTaskMsg( 'Импорт данных' );

for ( ... )
{
    ModalTaskMsg( 'Объект ' + i );
}

FinishModalTask();
```

В часто вызываемом коробочном коде данную функцию использовать не рекомендуется, ввиду наличия более удобного для пользователя механизма методов с прогресс-индикаторами. Функцию хорошо использовать в одноразовом или редко используемом коде ввиду его простоты.

## IsCancelError()

Проверяет, является ли заданный объект типа `Error` ошибкой отмены операции.

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `e` | `Error` | Объект типа Error |

### Результат

`Bool`

### Пример

```js
try
{
    HttpRequest( .... );
}
catch ( e )
{
    if ( ! IsCancelError( e ) )   
        alert( 'Невозможно активировать программу: ' + ExtractUserError( e ) );
}
```

## OpenCodeLib()

Функция `OpenCodeLib()` открывает библиотеку v1.

Функция открывает документ либо с расширением XML, содержащий набор методов, и возвращает его корневой элемент, либо с расширением `.JS`, содержащий функции, возвращает псевдо-документ (псевдо-форму), содержащую те же функции. Действие этой функции похоже на действие функции `EvalCodeUrl`, но не тождествены ей. Если мы выполняем сторонние функции при помощи `EvalCodeUrl`, то переменные из нашего кода видны внутри сторонних функций, что может привести к разрушению кода при совпадении названий переменных. `OpenCodeLib` лишен этого недостатка, т.к. приводит к загрузке функций из внешнего источника как независимых методов, каждый из которых выполняется в собственном окружении. Но в этом случае, в отличие от `EvalCodeUrl`, загружаемый js-файл не может содержать глобальных переменных, а только функции.

Загруженный документ помещается в кэш документов и при следующем вызове результат берется уже из кэша.

Возвращает объект типа `XmlElem`.

### Синтаксис

```js
OpenCodeLib( url )
```

### Аргументы

| Аргумент | Тип | Описание |
|---|---|---|
| `url` | `string` | url загружаемого документа с расширением XML или JS |

### Пример

```js
OpenCodeLib( 'lib_backup.js' ).RunBackup()
```
